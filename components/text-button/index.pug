input-field(value="{{ amount }}" type="{{ inputType }}" placeholder="{{placeholder}}")
    ack-button(
        class="right {{class}}"
        type="{{buttonType}}"
        on-click="_buttonclick"
        value="{{ value }}"
        disabled="{{ disabled }}"
        enabled="{{ enabled }}"
    ) {{yield}} 
