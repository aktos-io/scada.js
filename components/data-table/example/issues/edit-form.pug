.ui.segments 
    h3.ui.top.attached.block.header 
        | ID: #[b {{ curr._id }}]
        +apply('controls "right floated" as cls')
        div(style="clear: both")
        //- TIP: Use `curr._rev` to determine if this is a new document or not

    .ui.segment 
        json-edit(value="{{curr}}" style="height: 300px; width: 100%")
            
    .ui.segment 
        //- use an ack-button to save your document
        ack-button.green(on-click="save") Kaydet        
        btn.inverted.red.icon(on-click="closeRow"): icon.remove Kapat
        
