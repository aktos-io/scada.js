mixin if(condition)
    != '{{#if ' + condition + '}}'
    block
    != '{{/if}}'

mixin unless(name)
    != '{{#unless ' + condition+ '}}'
    block
    != '{{/unless}}'

mixin each(name)
    != '{{#each ' + name + '}}'
    block
    != '{{/each}}'

mixin partial(name)
    != '{{#partial ' + name + '}}'
    block
    != '{{/partial}}'

mixin component(name)
    != '<script id="' + name  + '" type="text/ractive">'
    block
    != '</script>'
