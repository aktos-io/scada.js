.input-group
    input.form-control(value="{{ amount }}" style="min-width: 50px" type="{{ type }}")
    span.input-group-btn
        ack-button(
            on-buttonclick="_buttonclick"
            value="{{ value }}"
            disabled="{{ disabled }}"
            enabled="{{ enabled }}"
            )
            | {{yield}}
