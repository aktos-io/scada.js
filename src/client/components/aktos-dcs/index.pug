.ui.segments
    h2.ui.top.attached.block.header aktos-dcs Test Zone
    .ui.attached.segment 
        p This section is for testing #[code aktos-dcs] library functions. 
        

    h3.ui.top.attached.block.header Subscribe to same topics 
    .ui.attached.segment 
        h3.dividing.header Test value (subscribed to #[code my-test-pin3])
        input-field(value="{{ testValue3}}")
        slider(value="{{ testValue3 }}")

    .ui.attached.segment 
        h3.dividing.header Test value2 (subscribed to #[code my-test-pin3])
        input-field(value="{{ testValue4}}")
        slider(value="{{ testValue4 }}")
    
    h3.ui.top.attached.block.header Subscribe to separate topics 
    .ui.attached.segment 
        h3.dividing.header Test value (subscribed to #[code my-test-pin1])
        input-field(value="{{ testValue}}")
        slider(value="{{ testValue }}")

    .ui.attached.segment 
        h3.dividing.header Test value2 (subscribed to #[code my-test-pin2])
        input-field(value="{{ testValue2}}")
        slider(value="{{ testValue2 }}")
