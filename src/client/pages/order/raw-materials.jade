h2 Hammaddeler:
order-table(
    settings="{{ rawMaterials }}"
    db="{{ db }}"
    view="primitives/raw-material-list"
    gen-entry-id="{{ genEntryId }}"
    )
    +partial('addnewTitle')
        | Hammadde Ekle
    +partial('header')
    //-+partial('viewForm')
        //-h3 Hammadde Adı: {{ curr.name }}
        //-h3 Kritik Değer: {{curr.criticalAmount}}
    +partial('editForm')
        panel(title="Müşteri" type="primary")
            //- {{ JSON.stringify(curr) }}
            form-vertical
                form-group
                    span Hammadde Adı: 
                    input(value="{{ curr.name }}" )
                    span Kritik değer: 
                    input(value="{{ curr.criticalAmount }}" )
            btn(on-buttonclick="newOrderClose") Kapat!
            btn(on-buttonclick="addNewOrderSave") Kaydet {{saving}}
