//processes.json:
    {
      "apps" : [{
        "name"        : "server",
        "script"      : "server/server.ls",
        "watch"       : "server/server.ls",
        "log_date_format"  : "YYYY-MM-DD HH:mm Z",
      }]
    }
