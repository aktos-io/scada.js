include ../../templates/mixins.jade
    
+partial('jq-flipswitch-v2')
  select(
    class="switch-button"
    data-role="slider"
    data-pin-name="{{ .pin_name }}"
    )
    option(value="off") Off
    option(value="on") On

