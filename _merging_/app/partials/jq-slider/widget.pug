include ../../templates/mixins.pug
    
+partial('jq-slider')
  .jq-slider.
    <input 
      value="{{ .val }}"
      class="jq-slider__container" 
      min="0" 
      max="100" 
      type="range"
       />

  
  
