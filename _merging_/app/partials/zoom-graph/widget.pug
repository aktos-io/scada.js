include ../../templates/mixins.pug

+partial('zoom-graph')
  .zoom-graph.
    <div class="zoom-graph__graph" id="{{ .actor_id }}" /> 
  .overview-graph.
    <div class="overview__graph" id="{{ .actor_id }}" />
